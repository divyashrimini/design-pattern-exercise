# Singleton Logger Example (Java)

This project demonstrates the **Singleton Design Pattern** in Java.  
The `Logger` class ensures that only **one instance** of the logger is created, no matter how many times you request it.

---

## 📂 Project Structure
